{% extends 'base.html.twig' %}


{% block main %}
    {% include 'elements/_video-player.html.twig' %}

    <div class="container">
        <div class="row">
            {% for video in videos %}
                {% include 'elements/_video.html.twig' %}
            {% endfor %}
        </div>
    </div>

{% endblock %}